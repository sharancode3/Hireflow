# REQUIRED in production (e.g. https://api.example.com)
# OPTIONAL in local dev when using Vite proxy (/api -> localhost:4000)
VITE_API_URL=

# REQUIRED to enable real Google sign-in button
VITE_GOOGLE_CLIENT_ID=
